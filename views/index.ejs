<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Baked Goods</title>
</head>
<body>
    <ul>
        <% bakedGoods.forEach(function(bakedGood) { %>
        <li><a href="/bakedGoods/<%= bakedGood.name %>"><%= bakedGood.name %> - $<%= bakedGood.price %></a></li>
        <% }); %>
    </ul>
    
</body>
</html>